//========================================================== MAIN APP ==========================================================

function App() {
}

App.prototype.run = function (server, port)
{
	console.log(arguments);
}
